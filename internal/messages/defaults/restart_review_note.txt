this is a restart, and your work had already entered review before the restart. Immediately run `git status` before taking any action and preserve every existing uncommitted change; do not use destructive cleanup such as `git checkout .` or `git reset --hard`. Perform a brief self-check and context alignment, inspect the worktree and message history, then promptly run `omo done` again to restart review unless that check reveals changes you need to make first.
